apiVersion: v1
kind: Pod
metadata:
  name: pod-test
  namespace: manish-namespace
spec:
  containers:
    - name: busybox
      image: radial/busyboxplus:curl
      command:
        - sleep
        - '3600'
